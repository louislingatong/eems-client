import '../../../scss/styles.scss';
import React from 'react';
import Page from '../../../src/modules/auth/pages/reset-password-token';

const ResetPasswordToken = () => {
    return (
        <React.Fragment>
            <Page />
        </React.Fragment>
    );
};

export default ResetPasswordToken;
